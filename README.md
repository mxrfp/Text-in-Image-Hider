# Text-in-Image-Hider
A Python tool to hide and decode text within images using Least Significant Bit manipulation. It embeds message length in the first row and saves as lossless PNG to ensure data integrity.
